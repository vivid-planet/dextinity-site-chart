{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dextinity-site.serviceAccountName" . }}
  labels:
    {{- include "dextinity-site.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "dextinity-site.serviceAccountName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: edit
subjects:
  - kind: ServiceAccount
    name: {{ include "dextinity-site.serviceAccountName" . }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dextinity-site.fullname" . }}-service-account-token
  annotations:
    kubernetes.io/service-account.name: {{ include "dextinity-site.serviceAccountName" . }}
type: kubernetes.io/service-account-token
{{- end }}
